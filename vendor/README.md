# External binary dependency

Only the [Linux x86-64 CSV library](csv/README.md) is included. Obtain WinDivert, Npcap, OpenSSL, libpcap, and Rust crates through the appropriate dependency/toolchain installation process. They are not copied into this directory.
