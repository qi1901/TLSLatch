# Hygon CSV library

`libcsv.so` is a Linux x86-64 shared library supplied by Hygon. Runtime dependencies: glibc, libdl, and libpthread.

Keep the file in this directory and verify it before building the [CSV wrapper](../../../csv/linux/README.md):

```sh
sha256sum -c SHA256SUMS
```
